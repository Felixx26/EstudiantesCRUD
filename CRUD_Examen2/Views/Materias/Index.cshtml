@model IEnumerable<CRUD_Examen2.Models.Materia>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nombre_materia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.creditos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.codigo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_materia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.creditos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.materia_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.materia_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.materia_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
